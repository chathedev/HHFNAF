// TODO: install @testing-library/jest-dom, then uncomment:
// import '@testing-library/jest-dom/vitest'

// Global test setup — extend as component tests are added (e.g. mocking
// matchMedia, IntersectionObserver used by embla-carousel/react-intersection-observer).
